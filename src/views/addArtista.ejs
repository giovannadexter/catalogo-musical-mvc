<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Adicionar Artista</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Adicionar Artista</h1>
  <form action="/artists/add" method="POST">
    <label for="name">Nome do Artista:</label>
    <input type="text" id="name" name="name" placeholder="Nome do Artista" required>

    <label for="genre">Gênero Musical:</label>
    <input type="text" id="genre" name="genre" placeholder="Gênero Musical" required>

    <label for="album_id">Associar a um disco existente (opcional):</label>
    <select id="album_id" name="album_id">
      <option value="">Selecione um álbum</option>
      <% albums.forEach(album => { %>
        <option value="<%= album.id %>"><%= album.title %></option>
      <% }) %>
    </select>

    <button type="submit">Adicionar Artista</button>
  </form>

  <div class="button-container">
    <a href="/" class="btn-back">Voltar para Home</a>
  </div>
  
</body>
</html>
