<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalhes do Álbum</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Detalhes do Álbum</h1>
  <div class="album-details">
    <img src="<%= album.cover_url %>" alt="<%= album.title %>">
    <h2><%= album.title %></h2>
    <p>Artista: <%= album.artist ? album.artist.name : 'Artista desconhecido' %></p>
    <p>Ano de Lançamento: <%= album.release_year %></p>
    <p>Gênero: <%= album.genre ? album.genre.name : 'Gênero desconhecido' %></p>
    <h3>Faixas</h3>
    <ul>
      <% if (album.tracks && album.tracks.length > 0) { %>
        <% album.tracks.forEach(track => { %>
          <li><%= track.name %></li>
        <% }) %>
      <% } else { %>
        <li>Nenhuma faixa encontrada.</li>
      <% } %>
    </ul>
    
    <div class="actions">
      <% if (album.artist) { %>
        <a href="/artists/<%= album.artist.id %>" class="btn btn-primary">Ver Detalhes do Artista</a>
      <% } else { %>
        <p>Artista não disponível</p>
      <% } %>
      
      <a href="/albums/<%= album.id %>/edit" class="btn btn-warning">Editar Informações</a>
      <a href="/" class="btn btn-primary">Voltar para Página Inicial</a>
    </div>
    
  

</body>
</html>
